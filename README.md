# News-Category-Classifier
This project implements a News Category Classifier that automatically classifies news articles into different categories using Natural Language Processing (NLP) and Machine Learning techniques.

The model processes news text, cleans and tokenizes it, and then predicts the most likely category based on learned word frequencies.

The project is implemented in Python inside a Jupyter Notebook.
